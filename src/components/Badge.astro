---
export interface Props {
	color?: string
}

let color = Astro.props.color

if (!color) {
	color = 'bg-blue-100 text-blue-800 dark:bg-blue-900 dark:text-blue-300'
}
---

<span class=`${color} me-2 rounded-md  px-2.5 py-0.5 text-sm font-medium -900 `>
	<slot />
</span>
